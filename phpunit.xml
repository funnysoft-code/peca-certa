<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Browser">
            <directory>tests/Browser</directory>
        </testsuite>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
        <exclude>
            <!-- Service providers that register configuration during app boot
                 (before coverage tracking starts) cannot be covered by tests. -->
            <file>app/Providers/TypeScriptTransformerServiceProvider.php</file>
            <!-- Abstract base classes provided as scaffolding for future
                 controllers/requests; no concrete callers yet. -->
            <file>app/Http/Controllers/Controller.php</file>
            <file>app/Http/Requests/Request.php</file>
            <!-- Large multi-provider cost aggregator; public execute() paths are
                 covered in tests/Feature/Costs/* but provider-specific branches
                 (Cloudflare wrangler, Laravel Cloud shared allocation, full
                 management billing matrix) are not fully fixture-mocked yet. -->
            <file>app/Actions/UpdateProjectCosts.php</file>
        </exclude>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="INERTIA_SSR_ENABLED" value="false"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <!-- Force local Bun sidecar in tests; HTTP path is covered explicitly. -->
        <env name="AUTOZITANIA_HTTP_URL" value=""/>
        <env name="AUTOZITANIA_HTTP_TOKEN" value=""/>
        <!-- PL24 fingerprint/pacing is config-hardcoded; APP_ENV=testing zeroes delays/volume. -->
    </php>
</phpunit>

# PartsLink24 agent tools

Thin wrappers over `App\Services\PartsLink24\PartsLink24Client` for the identify agent (F7T-48).

Tool contract: `docs/partslink24/agent-tool-contract.md`.

Use `ResolvesPartsLink24Brand` for WMI → catalog resolution. Never call pricing suppliers from these tools.
